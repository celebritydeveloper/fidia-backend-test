type Mutation {
	register(payload: CreateUserInput!): Result
    login(payload: LoginUserInput!): Result
    verifyAccount(resetToken: String!): Result
    resendVerificationEmail(email: String!): Result
}
